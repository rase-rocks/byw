const attribution = {
    attribution: "&copy; <a href=\"https://www.openstreetmap.org/copyright\">OpenStreetMap</a> contributors"
};

const tileLayerString = "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png";

const initialCoords = [53.06, -4.07];

const MAP_ID = "full-page-map";

export {
    attribution,
    tileLayerString,
    initialCoords,
    MAP_ID
};