//import PropTypes from "prop-types";
import React from "react";

import LocatorMap from "./locator-map";

class LocatorMapController extends React.Component {
    render() {
        return (
            <LocatorMap/>
        );
    }
}

LocatorMapController.propTypes = {

};

export default LocatorMapController;